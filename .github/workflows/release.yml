name: Release

on:
  push:
    tags: ["v*"]
  workflow_dispatch:
    inputs:
      tag:
        description: "Release tag to create (e.g. v1.0.0)"
        required: true
        default: "v1.0.0"

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    env:
      TAG: ${{ github.event_name == 'workflow_dispatch' && inputs.tag || github.ref_name }}
    steps:
      - name: Check out
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Build and test
        run: ./gradlew --no-daemon test assembleDebug assembleRelease

      - name: Name the APKs after the tag
        run: |
          cp app/build/outputs/apk/debug/app-debug.apk \
             "colordict-${TAG}-debug.apk"
          cp app/build/outputs/apk/release/app-release-unsigned.apk \
             "colordict-${TAG}-release-unsigned.apk"
          # Stable filenames too, so /releases/latest/download/colordict-debug.apk
          # keeps working as versions change (used by the download site).
          cp app/build/outputs/apk/debug/app-debug.apk colordict-debug.apk
          cp app/build/outputs/apk/release/app-release-unsigned.apk \
             colordict-release-unsigned.apk

      - name: Publish GitHub release with APKs
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "${TAG}" \
            "colordict-${TAG}-debug.apk" \
            "colordict-${TAG}-release-unsigned.apk" \
            "colordict-debug.apk" \
            "colordict-release-unsigned.apk" \
            --target "${GITHUB_SHA}" \
            --title "Pop Up Dictionary ${TAG}" \
            --notes "Ready-to-install **debug APK** (install this one directly on your device) and an unsigned release APK (sign it with your own key before distributing)." \
            --repo "${GITHUB_REPOSITORY}"
