# Overnight 2026-09-26 (UTC+8)

**Status: RESUMED 03:34 UTC+8** ("resume, add the floor"). Stopped 03:25 on census 014, 1 of 13 = 7.7%, after finishing tick 1 and the pilot. The author said go ("Gooo!") at 03:06 UTC+8 - a plain go, so the run only: no `CLAUDE.md`, no hook, no learner reader. plain_lint and census-reader check the pilot.**

Written at 03:01 UTC+8, before a compaction the author asked for, so that nothing
decided tonight lives only in the conversation. After a compaction, read this
file and run `/orient` before acting.

## What "yes to all, go" approves

1. **`CLAUDE.md` at the repo root, and a compaction hook.** The hook is a
   `SessionStart` hook with matcher `compact` in `.claude/settings.json` that
   runs `python tools/status.py --quick` (0.5 s, ~9.5 KB). Both drafts are at the
   end of this file. The deny rule on `.claude/agents/**` stays byte-identical.
   Claude Code re-reads `CLAUDE.md` from disk after every compaction; skills
   loaded earlier (`/orient`) are dropped. Hooks are read when a session starts,
   so the hook takes effect from the next session.
2. **A new instrument, `learner-reader.md`, for stage 9b only.** It reads each
   rewritten note blind, as a learner of English would, marks it easy or hard,
   and quotes the words that would stop a learner. `model: fable`. The
   simplifier is opus, so the hand that writes never reads. It is drafted,
   reviewed by a second agent, then placed the way the author's 2026-09-26
   unlock placed three: lift the deny rule, copy the file in, restore the rule
   the same minute. After that, `settings.json` must be byte-identical to HEAD
   and the file sha256-identical to its draft. It goes into
   `instrument_gate.py`, and the plan records a new baseline.
3. **The night run below.**

A "go" without "yes to all" approves only the run. In that case plain_lint and
census-reader check the pilot, there is no learner reader, and the files wait.

## Decided tonight by the author

- Agents check the pilot instead of the author: "instead of me checking can you
  have other agents check it?" The author is not the test reader. Reading 5
  before/after pairs in the morning is optional, and nothing waits for it.
- The orchestrator stays on Opus 5.5 at max effort. The instrument files pin
  every other model.
- www.gutenberg.org is reachable, and book one fetches byte-identical to
  `3f6bb9d6f78e0293` (commit aeda765).

## The plan for the night

Times count from the go. At 03:00 that ends around 07:00.

| from go | work | stage 9b step |
| --- | --- | --- |
| 0-30 min | set up: `CLAUDE.md` and hook, learner reader, rebuild `data/source`, `data/build` and `derived-bulk.jsonl` (runbook 0.2, all absent in this checkout), `pip install wordfreq`, arm the watchdog | - |
| 30-60 min | pilot: one simplifier packet, drawn clear of the six carried-fault families, checked by the learner reader, census-reader and plain_lint | pilot |
| 30-90 min, in parallel | tick 1: the six carried faults (runbook tick 1) | 4 |
| after both | tick 2: the full plain-words pass (runbook tick 2), ~29 simplifier packets | 5-9 |
| at the end | morning report in this file; commit and push | 10 if it passed |

No tick starts after 06:30. Everything stops by 08:00. Queue ticks do not fit
tonight.

## Stops

- **The pilot fails.** Fewer than 9 in 10 notes read easy to the learner
  reader, or the pilot census is at 5% or over. Then there is no full pass:
  report, and propose a simpler rule.
- **Any census at 5% or over** - from 50 read senses up (floor added 03:34, below).
- **Agents fail on usage or rate limits twice**, ten minutes apart.
- **A git lock is held by a live process for over 15 minutes.**
- **08:00 UTC+8.**

Nothing reaches `main`. No tag, no release, no APK. Stage 10 is not approved.

## Log

Clock times after the go were not recorded as they happened; the order is exact. The whole run took 03:06-03:25.


- 03:06 go. status clean, 0 unpushed, instruments unchanged, no locks, gate pass.
- runbook 0.2: sources fetched (OEWN 9ca6d1dc, SWN 4fc9b253, ili 13b67413, all as recorded), derived-bulk and both family files rebuilt, `dict_pipeline --no-build` 0 errors, git status clean. wordfreq installed.
- tick 1 and the pilot set up, 4 agents launched in parallel:
  - `data/policy/carried-001/repairs-input.json`: the 4 superlative collisions (feeling, impression, unreformable, peccable), fault `other`, lint message as `reader_said`, whole family attached -> 1 `repairer`.
  - The 2 stale axes: family-10008828-n and family-05911139-n copied from draft-018 into `draft-019r.json`; removed from `annotated-018.json` by script, 87 -> 85 families, every other family asserted identical (the diff is pure deletion). `packets-019r` -> 2 `family-author`. Read for §5.3: forms of address (lady, ma'am, madam, dame, gentlewoman) and scheme/plan words; none is a slur or a group label, so no hold.
  - Pilot: `plain-000-pilot`, `draw --limit 11 --per-packet 11` = the first 11 families (annotated-001 onward), 16 notes; none from the fault families -> 1 `simplifier`.
  - Watchdog armed for 04:42.
- the author: **"Yes to all, go!"** - approves `CLAUDE.md`, the compaction hook and the learner reader.
- `CLAUDE.md` written from the draft below; `.claude/settings.json` gains the `SessionStart`/`compact` hook, deny line byte-identical to HEAD (takes effect next session).
- tick 1 agents back: family-author 10008828-n 11,183 tokens, 05911139-n 10,588; repairer 21,104. `family_merge` 2/2 families, 9 senses; tone_lint 0/9; `plain_lint --max-long 0` 0/9 (median 15 words, longest 20).
- repairer: tone on all 4, 0 keeps, no charge moved; each now names one neighbour instead of ranking. It noted peccable's "mildest" was also false as printed (heavy sits at charge 0). census_apply, family_apply on 018, 018c, 019r, `dict_pipeline --no-build` 0 errors; `tone_lint --all` superlative-collision 4 -> 0.
- learner-reader.md drafted (`data/policy/instrument-drafts/`), reviewed by a second agent (76,917 tokens): "place with the listed changes"; changes 1-8 taken (hard-word rule aligned with simplifier.md/plain_lint's two-word limit, the lint's grammar list, no hint that notes are rewrites, a B2 learner with two calibration notes, vivid words are not figures, truth kept out of `why`, typed `stops`, packet number in output). Change 9 (seeded controls) is for whoever builds the learner packets.
- **placing it was refused**: the session's auto-mode classifier denied lifting the deny rule on `.claude/agents/**` (self-modification). Not worked around. The draft waits in `data/policy/instrument-drafts/learner-reader.md`; the author places it by hand (lift the rule, copy, restore, add to `instrument_gate.py` INSTRUMENTS). Tonight's pilot therefore falls back to plain_lint + census-reader, as a plain go would.
- pilot simplifier back (39,907 tokens): `check` 16 tone, 0 keeps, 0 lint warnings. Applied: census_apply, family_apply on every shard (overlays 001-005 change), `dict_pipeline --no-build` 0 errors.
- blind reads out, 1 `census-reader` each: `census-014` (tick 1: the 9 re-authored senses of 019r + the 4 repairs, read complete) and `census-013p` (the 16 pilot rewrites).
- census-014 read (14,393 tokens): **12 right, 1 wrong** - `maam.oewn-10008828-n`, provenance: "A shortened *madam*" is an origin story. noun stratum 1/11 = 9.1%, adjective 0/2. **The stop fires: any census at 5% or over.** The step in hand was finished: third-hand `repairer` (12,379 tokens) -> "Carries the same respect as *madam* for a refined woman, but said more softly and briefly." applied; both lints clean on 019r; a fresh `census-reader` (10,350 tokens) re-read it: right.
- census-013p read (18,980 tokens): **16 right, 0 wrong** - the pilot passes on truth. Its `why` fields noted *asleep* and *pass* both claim "the gentlest" way to say dead. That clash predates tonight (the old notes said "the gentlest euphemism" and "the gentlest form of the euphemism") and spans an adjective and a verb family, so tone_lint cannot see it.
- Run stopped. No full plain-words pass.

## Morning report

**Why it stopped:** census 014 read 1 of 13 wrong (7.7%, noun stratum 9.1%), over the 5% gate. One fault in a population of 13 moves the rate 7.7 points, so a single fault on a small tick fires the rule; that is the rule as written, and it held. The fault was repaired third-hand and passed a blind re-read.

**Tick 1 - the six carried faults (stage 9b step 4): done.**
- 4 superlative collisions (feeling, impression, unreformable, peccable): 1 repairer, 4 tone, 0 keeps, no charge moved. tone_lint superlative-collision 4 -> 0; tone_lint total 19 -> 15.
- 2 stale axes: families 10008828-n and 05911139-n re-authored from scratch as annotated-019r (9 senses); annotated-018 87 -> 85 families, the rest byte-identical. Both lints 0/9.
- census 014: 13 read, 1 wrong (provenance, ma'am) -> repaired -> re-read right.
- Agents 6 (2 family-author, 2 repairer, 2 census-reader), 79,997 subagent tokens. Pilot 2 agents, 58,887. The learner-reader review 76,917.

**Pilot (plain words, before step 5): passed on the fallback checks.**
- plain-000-pilot: the first 11 families, 16 notes flagged. 1 simplifier (39,907 tokens): 16 tone, 0 keeps, 0 lint warnings.
- census 013p: 16 read, 0 wrong.
- No learner reader, so "easy to read" was measured only by plain_lint.
- plain_lint --all: 407 -> 497 flagged, because wordfreq was installed tonight and hard-words ran for the first time (127 before the pilot, 119 after). too-long 274 -> 270, grammar 146 -> 138.

**Set up:** `CLAUDE.md` and the `compact` SessionStart hook are committed (the hook works from the next session). Data rebuilt byte-identical from the three sources.

**What needs the author:**
1. **Place `learner-reader.md`.** It is drafted and reviewed, and waits in `data/policy/instrument-drafts/`. Lifting the deny rule was refused to this session. Copy it into `.claude/agents/` yourself (GitHub web works from a phone), then the next session adds it to `instrument_gate.py` and records the new baseline.
2. **The stop rule on small ticks.** At n=13, a single fault is 7.7%. Keep it as is, or set a floor (e.g. the rate decides only at n >= 50, and below that any fault is repaired and re-read)? Your call.
3. ***asleep* / *pass*, both "the gentlest".** An old cross-family clash, left standing; route it to a repairer next time?
4. **Next night:** tick 2, the full plain-words pass. `draw` gives ~29 packets from the current corpus; the pilot's 11 families are done and will not be redrawn. Suggested cap unchanged: 4 ticks.

Nothing reached `main`. No tag, no release, no APK.

## Drafts awaiting approval

### `CLAUDE.md`

```markdown
# ColorDict

A connotation dictionary for learners of English. The plan is
docs/BUILD-PLAN.md; `python tools/status.py` measures where it stands.

## At the start of a session, and after every compaction

1. Run `/orient`, or at least `python tools/status.py --quick`, before you say
   anything about state. A summary is memory, not state: when it disagrees with
   status.py, status.py is right.
2. During an overnight run, read tonight's log, `docs/overnight/<YYYY-MM-DD>.md`
   (dated in UTC+8), before you spawn anything. An agent that is still running,
   or already wrote its file, must not be spawned again.

## Rules that do not bend

- The hand that writes never reads. One agent writes, a blind reader reads, and
  a third agent repairs.
- Instruments are the files in `.claude/agents/`, spawned by name and used
  verbatim. Never retype a rubric into a prompt.
- Agents write their output to disk and return one line. Do not paste their
  results or big files into the conversation; that is what fills the context.
- Never `git add -A`; stage explicit paths. Commit and push after every tick.
- A census over 5% stops the run.
- A decision the author makes goes into `data/policy/build-stages.json` the same
  hour. The next session cannot see this conversation.
```

### `.claude/settings.json`

```json
{
  "permissions": {
    "deny": [
      "Edit(.claude/agents/**)",
      "Write(.claude/agents/**)"
    ]
  },
  "hooks": {
    "SessionStart": [
      {
        "matcher": "compact",
        "hooks": [
          {
            "type": "command",
            "command": "python tools/status.py --quick"
          }
        ]
      }
    ]
  }
}
```

## After the run

- The author placed `learner-reader.md` by hand (commit 2b358d1). Verified sha256 `4378d289` identical to the reviewed draft; `settings.json` untouched. Added to `instrument_gate.py` INSTRUMENTS (nine instruments now) and recorded in build-stages.json 9b `pilot`. **The next pass that uses it is a new baseline.** The pilot's 16 rewrites have not been read by it; the next night can do that first, before step 5.

## Resumed

- 03:34 the author: **"resume, add the floor"**. The 5% stop now decides only from 50 read senses up; below that every fault is repaired third-hand and re-read blind, and the run goes on. Written into the overnight runbook's hard stops and build-stages.json 9b. Census 014 (1/13) stands as recorded; under the floor it would have been repaired and re-read, which it was.
- 03:36 `tools/learner_packets.py` written: `draw` builds learner-reader packets from a pass's `tone` decisions (checked against what ships) plus unlabelled controls - notes plain_lint flags for hard-words or grammar (should read hard) and short unflagged notes of everyday words only, zipf >= 4 (should read easy) - seed 2026, shuffled, key kept apart in key.json; `aggregate` scores the easy rate over rewrites alone, and a pass needs >= 90% easy AND every control right (the reviewer's change 9). The first draw was discarded before any reader saw it: an "easy" control (*mincingly*, "daintiness") was not plainly easy, so easy controls were tightened to everyday words.
- 03:37 `learner-000-pilot`: the 16 pilot rewrites + 2 hard and 2 easy controls, 20 notes -> 1 `learner-reader`. First use of the instrument: a new baseline.
- 03:37 `plain-001` drawn: 219 families, 497 notes to rewrite (too-long 270, grammar 138, hard-words 119), 28 packets. The simplifiers wait for the pilot's learner read. Watchdog re-armed for 05:06.
- learner-000-pilot read (14,068 tokens): **16/16 rewrites easy (100%), all 4 controls right** (corrupted and jingoistic hard, questioning and preservable easy). **Pilot passes.** 28 simplifiers out on plain-001.
- plain-001 packet 02: the simplifier flags *disciplinary* - the note calls it neutral, the charge is -1; the meaning was kept and the disagreement left for the census reader.
- plain-001 packet 10: *enthusiastic* note reads near-neutral against charge +1 (left for the census reader); misplaced, contrastive and well-spoken each dropped one of two contrasts, as simplifier.md allows.
- plain-001: 28 simplifiers (20 at a time, the session's concurrency cap). `check` first refused 3 errors: packet 03 keyed a precedential decision to the wrong synset, packet 17's *unheeded* ran 25 words. Each went back to its own simplifier (no new hand), then `check`: **497 tone, 0 keeps, 2 hard-words warnings** (arrant, apology).
- Applied: census_apply 497, family_apply on every shard, `dict_pipeline --no-build` 0 errors. **plain_lint --all: 497 flagged -> 2** (too-long 270 -> 0, grammar 138 -> 0, hard-words 119 -> 2); longest note 24 words, median 18. tone_lint 15 -> 20 (hedge-claim 8 -> 11, etymology 2 -> 4): most new flags read as false positives on sight ("blaming no one", "no one to answer to"); the census decides.
- census-013 drawn: all 497 rewrites, 18 `census-reader` packets. learner-001: 497 rewrites + 4 hard and 4 easy controls, 2 `learner-reader` packets. 20 readers out.
- **census-013: 497 read, 8 wrong = 1.6%** (adjective 7/395 = 1.8%, noun 0/77, verb 1/25 = 4.0% - under 50, so the floor applies). Faults: restriction 4, gloss-mismatch 3, wrong-charge 1 (*disciplinary*, the one packet 02's simplifier had flagged). Under 5%: no stop. Reader 14 first left an unparseable file with a placeholder line; it finished and rewrote it clean before the aggregate. 18 readers.
- The 8 faults -> 1 `repairer` (`census-013-repairs-input.json`).
- census-013 repairs (1 `repairer`, 33,477 tokens): 7 tone, 1 charge; charges moved on amplify (-> -1), disciplinary (-1 -> 0, note and charge now agree), intelligent (-> 0). Applied; `dict_pipeline` 0 errors; plain_lint still 2. Re-read out: 1 fresh `census-reader` on all 8.
- **learner-001: rewrites 485/497 easy = 97.6%** (hard: polite, stung, hurt, arrant, discreetness, reassured, wickedness, priggish, anile, colour, lacerated, +1). **But the aggregate says FAIL**: 3 of 4 hard controls read easy. Looked at them: obscenely, utterly, unfeignedly were "hard" only because plain_lint's zipf test counts *adverb* as rare; each has one real hard word (indecency / sneer / pretence), which learner-reader.md allows. The reader applied its rubric; the control draw was wrong. The FAIL stands in results.json. Fix in `learner_packets.py`: a hard control needs a grammar term or two rare words besides school words (*adverb* etc.); after the pass almost no shipped note is hard, so `--hard-from <rev>` draws hard controls from the notes before tonight (2ba4e29). Calibration re-check `learner-001c`: 6 hard + 6 easy controls only, fresh `learner-reader`. The 97.6% counts only if it gets them right.
- learner-001c (calibration, 12,660 tokens): **11 of 12 controls right** - 5/6 hard read hard, 6/6 easy read easy. The miss: *hazardously* ("formal and classifying - it registers danger as a property to be assessed, cooler than *risky*"), read easy; borderline, since "registers" and "classifying" are ordinary words used abstractly. By the tool's rule (every control right) this is FAIL too, and it is recorded as such. **Not re-run**: a third draw until it passes would be fishing. For the author: the reader is well calibrated on easy notes and caught 5 of 6 hard ones, so the 97.6% may be a little generous, not much. Whether step 9's readability bar is met is the author's call.
- census-013 re-read (13,002 tokens): **8/8 right**.
- **Stage 9b done-check passes** (03:54): every flagged note rewritten (513 with the pilot, 0 keeps), 0 notes over 24 words, census 013 1.6%. Steps 5-10 written into build-stages.json with measured numbers; closing the stage is the author's.

## Morning report - after the resume

**Stage 9b's done-check passes.** Tick 2, the plain-words pass, ran 03:34-03:54.
- 497 notes rewritten by 28 simplifiers, 0 kept. plain_lint 497 -> 2 flagged; no note over 24 words (median 18).
- Truth: census 013 read all 497 blind, **8 wrong = 1.6%** (the usual ~2%). Repaired third-hand, re-read 8/8 right.
- Ease: the learner reader read **485 of 497 easy = 97.6%** (bar 90%). Its calibration is imperfect: my first control draw was wrong (3 "hard" controls hard only by *adverb*), and the re-check got 11 of 12 (missed *hazardously*). Both are recorded as FAIL by the strict all-controls rule, and neither was re-run to get a pass.
- Agents after the resume: 1 learner (pilot) + 28 simplifiers (+2 fix-ups) + 18 census-readers + 2 learner + 1 repairer + 1 census re-reader + 1 learner calibration.

**What needs the author (updated):**
1. **Close stage 9b?** The done-check passes. Your call on whether the learner read's 11/12 calibration is good enough, or whether you want a human look at ~10 before/after pairs first.
2. The 12 notes the learner reader still found hard (polite, stung, hurt, arrant, discreetness, reassured, wickedness, priggish, anile, colour, lacerated, +1): a small second simplifier round, or leave them.
3. *asleep* / *pass*, both "the gentlest": still standing.
4. tone_lint rose 15 -> 20 with the rewrites; the census passed all of them, but a repairer could look.
5. Queue ticks: the runbook allows adjective ticks once 9b is done, but tonight's plan said they would not fit. It is 03:54. Not started without your word.
- 05:08 watchdog check-in: status clean, 0 unpushed, gate pass, nothing in flight, no stop fired. The approved night (tick 1, the pilot, the plain-words pass, the report) is complete; queue ticks, a second pass on the 12 hard notes and closing 9b wait for the author, so nothing was started and the watchdog was not re-armed. `CLAUDE.md`'s census line now carries the author's floor, so a fresh session does not stop on a 1-in-13 again.

## 05:40 - the author closes 9b and approves a clean-up

The author, 05:40: "Close 9b, and go clean-up!"

- **Stage 9b closed** on its step-10 done-check (973fce4), written into build-stages.json the same hour. The author accepted the learner read as it stands. Before asking, I looked at the one calibration miss: *hazardously* was a hard control only because plain_lint read "registers" (a plain verb there) as the grammar term *register*. Its other rare words (*classifying*, *assessed*) make it borderline, not a clear reader failure.
- **Clean-up approved**, four jobs:
  1. a second simplifier round on the 12 rewrites learner-001 read hard;
  2. a repairer on *asleep* / *pass*, both "the gentlest" way to say dead;
  3. a repairer looks at the 5 tone_lint flags the pass added (15 -> 20), fixing or declining each;
  4. status.py follows the census floor (census 014 still printed OVER GATE at 13 read).
  Every changed note is read blind again, for truth (census-reader) and ease (learner-reader). The read is under 50 senses, so faults are repaired third-hand and re-read, per the floor.
- 05:45 `status.py` follows the floor (4170941): a census over 5% prints OVER GATE only from 50 read senses up; census 014 now reads "(over 5% under the 50 floor: repair, no stop)".
- Clean-up set up. Two hands, no family shared between them:
  - `plain_packets.py draw --learner-hard data/policy/learner-001/results.json` -> `data/policy/plain-002`: 10 families, the 12 hard notes marked `rewrite`, each `flagged_for` what stopped the learner reader (its `word` stops as `hard-words`; `sentence` and `figure` by name). 2 packets -> 2 `simplifier`.
  - `data/policy/cleanup-001/repairs-input.json`, 8 senses -> 1 `repairer`: *asleep* and *pass* (fault `other`, census-013p's blind `why` as `reader_said`, the other note attached as `collides_with`), and the 6 notes the pass newly flagged in tone_lint (it also cleared 1, *tool*, so 15 -> 20 is net +5): clear, despairing, fair, outclassed, unaccountable, unnoticed, fault `other`, the lint message as `reader_said`.
  - New tool options: `plain_packets draw --learner-hard`, `plain_packets census --also`, `learner_packets draw --also`, so one blind read of each kind covers every note the clean-up changes.
  - **In flight: 2 simplifiers (plain-002 decisions-01/02), 1 repairer (cleanup-001/repairs.json).**
- plain-002 simplifiers back: packet 01 8 tone, 0 keeps (32,197 tokens); packet 02 4 tone, 0 keeps (22,372 tokens). Packet 02 said the `sentence` and `figure` flags are kinds its rubric does not list, and it rewrote those notes anyway, since they were marked. `check`: 12 tone, 0 keeps, 0 lint warnings, 12-23 words.
- cleanup-001 repairer back (43,138 tokens): 4 tone (asleep, pass, clear, fair), 4 keep, no charge moved. *asleep* and *pass* each drop "the gentlest" for one contrast (*gone*; *pass away* with "away" dropped). *clear*: the lint misfired (the clause widens), but the repairer found the clause claimed performances the gloss does not cover, and rewrote it. *fair*: "no one can argue with" was a claim about hearers; cut. Keeps: despairing and outclassed ("comes from" states a cause, not an origin) and unaccountable and unnoticed ("no one" is the meaning). The repairer asks that tone_lint's patterns be narrowed. It also doubts *unaccountable*'s charge 0 and "not judged for it", and did not act, since the note was not marked. Both go to the author.
- Applied: census_apply plain-002 (12 tone) and cleanup-001 (4 tone, 4 keep), family_apply on every shard, `dict_pipeline --no-build` 0 errors. Adverbs re-inherited: fairly, priggishly. plain_lint --all 2 -> 1 (arrant cleared; apology stands, read easy by learner-001). tone_lint --all 20 -> 19 (narrowing 5 -> 4; fair's new note still trips hedge-claim on "nobody was favored").
- Reads drawn, gate pass: `census-015` (the 16 changed notes, 1 packet) and `learner-002` (the 16 plus 4 controls: better_off and drab hard, perish and omniscient easy; checked by hand before any read). **In flight: 1 census-reader, 1 learner-reader.**
- census-015 read (16,797 tokens): **15 right, 1 wrong = 6.2%** (adjective 1/12, noun 0/3, verb 0/1). *lacerated*, distribution: the simplifier turned "the ordinary *-ed* form" into "the usual *-ed* form", a frequency claim; tone_lint's pattern catches "usually" but not "usual". 16 read is under the 50 floor, so no stop: repaired third-hand and re-read blind.
- learner-002 read (17,295 tokens): **15/16 easy = 93.8%, controls 4/4, PASS.** All 12 plain-002 rewrites read easy. Hard: *fair* (the repairer's note), sentence: "without the weight of principle *just* carries". The clause has no "that", and the reader reads "principle just carries" first.
- Fix round, two hands again: *lacerated* -> 1 `repairer` (`census-015-repairs-input.json`, the reader's own `why`); *fair* -> `plain-003`, drawn with `--learner-hard data/policy/learner-002/results.json`, 1 `simplifier`. Then one blind re-read of each kind over the 2 changed notes. **In flight: 1 repairer, 1 simplifier.**
- Fix round back: plain-003 simplifier (12,469 tokens) 1 tone. *fair* is now "Plain approval that nobody was favored or tricked and the rules were kept - lighter than *just*, which carries the weight of principle." The census-015 repairer (15,119 tokens) gave 1 tone. *lacerated* is now "Sounds formal, almost medical, beside *torn*, and looks harder at the wound, bringing its jagged, ripped edges into sharp view." Applied, `dict_pipeline --no-build` 0 errors. plain_lint --all 1, tone_lint --all 19 (unchanged).
- Re-reads drawn, gate pass: `census-015-reread` (the 2 notes) and `learner-002r` (the 2 plus the same 4 controls, fresh reader). **In flight: 1 census-reader, 1 learner-reader.**
- census-015-reread (10,695 tokens): **2 right, 0 wrong**. learner-002r (12,049 tokens): **2/2 easy, controls 4/4, PASS.**

## Clean-up report

**Done.** 9 agents, 182,131 tokens. Every note the clean-up changed was read blind for truth and for ease, and passes both.
- **The 12 hard notes:** all 12 rewritten (plain-002) and all 12 now read easy. One, *lacerated*, came back wrong on truth ("the usual *-ed* form" is a frequency claim). It was repaired third-hand and re-read right and easy.
- ***asleep* / *pass*:** neither claims "the gentlest" any more. Each names one neighbour instead (*gone*; *pass away*).
- **tone_lint's new flags:** 6 notes (net +5). 2 were rewritten (*clear*, *fair*), and 4 were kept as pattern misfires (*despairing*, *outclassed*, *unaccountable*, *unnoticed*). *fair* then read hard, so it was re-simplified, and it now reads right and easy.
- **status.py** follows the floor. Census 015 (1 in 16, 6.2%) was under it: repaired and re-read, no stop.
- plain_lint --all 2 -> 1; tone_lint --all 20 -> 19.

**What needs the author:**
1. **Inherited adverb notes run long.** 133 of the 516 adverb notes are over 24 words, and **every one is over only because of the prefix "The adverb of *X*:"**; without it, 0 are. plain_lint --all does not read the adverb overlay, so the PLAIN WORDS block never counted them. The options: exempt the prefix from the count, shorten it, or show it as a label apart from the note. All three are cheap; none needs agents.
2. **tone_lint patterns.** The repairer asks that "comes from" (a cause, not an origin) and "no one" (part of the meaning) be narrowed: 4 of its 6 flags were misfires. And "usual" slipped past the distribution pattern, which catches only "usually"; census 015's one fault was exactly that.
3. ***unaccountable*:** the repairer doubts its charge of 0 and the phrase "not judged for it". It was not marked, so nothing was done.
4. *apology* still trips plain_lint (regretful, scornful). learner-001 read it easy, so it was left.

## 06:22 - the author's picks after the clean-up

The author asked what I needed and answered four questions:
1. **Adverb notes: a label above the note.** The note becomes the adjective's note, and "from *X*" shows above it. 133 of 516 inherited adverb notes ran over 24 words only because of "The adverb of *X*:" (0 without it; a 1-word prefix would leave 17 over, a 2-word one 46).
2. **Ease-read the 20** changed notes never read for ease (tick 1's 13 and 7 census-013 repairs): 1 learner-reader.
3. **Tune tone_lint:** catch "usual" as it catches "usually", and stop flagging "comes from" and "no one" where they carry the meaning. Not chosen: a blind recheck of *unaccountable*'s charge.
4. **Stage 10 approved** for the next night: 4 rounds a night, stop by 08:00, nothing to main. It starts on the author's go.
All four are recorded in build-stages.json.
- `learner-003` drawn: the 20 notes changed since a30598f whose shipped text no learner reader has seen (`ids.json`, found by comparing every learner packet's text with what ships), plus the same 4 controls. New option: `learner_packets draw --ids`. **In flight: 1 learner-reader.**
- learner-003 read (17,469 tokens): **18/20 easy = 90.0%, controls 4/4, PASS** (right at the bar). Hard: *tawdry* (idioms: "fixes on poor work passed off as sound", "written off whole") and *unreformable* ("reform will not take"). Not re-simplified: that needs the author's word. Proposed as the next night's first job.
- tone_lint tuned (the author's pick 3):
  - distribution now catches "the usual X", but not "the usual or expected";
  - etymology's "comes from" fires only as an origin ("the word comes from", "comes from the Latin");
  - hedge-claim fires only when "everyone / nobody / no one / anybody" is followed by a verb of saying, knowing or judging, or a modal ("no one can argue with").
  Tested on the known cases (the census-015 fault, the old *fair* note, both kept "usual" notes). `--all`: 19 -> 6 flags. 13 misfires are gone (10 hedge-claim, 3 etymology), no new ones. Left: *friendly* ("nobody objects to"), *joy* ("named for") and 4 narrowing.
- 06:30 adverb label (the author's pick 1). `adverb_inherit.py` no longer rewrites the note as "The adverb of *X*: ...". The adverb carries its adjective's note as written, plus `tone_from`, and `dict_build.py` shows that as "from *X*", a link in the label style the app already uses for *formal* and *British*. The field is declared in `dict_schema.json`, `dict_validate.py` and `dict_enrich_apply.py`. `census_packets.py` shows it to readers, so a census reader sees what the learner sees. Rebuilt: 516 adverb notes, 0 prefixed, **0 over 24 words** (was 133); validation 0 errors, warnings unchanged at 6601. The app's own assets are not rebuilt (no release tonight); the label appears at the next build.

## 06:35 - a daytime run: 4 hours, stage 10 begins

The author, 06:35: "Can we run for next 4 hours again?" Recorded in build-stages.json (stage 10, `go_2026_09_26`). **Stop by 10:35 UTC+8.** The night's 08:00 stop does not apply to a run the author starts in the morning.

Plan, in order:
1. *tawdry* and *unreformable* (read hard in learner-003): 1 simplifier (`plain-004`), then a blind census read and a blind learner read.
2. Stage 10 set-up: fetch book one (hash must be `3f6bb9d6f78e0293`), ingest it, measure the population (words and senses with no verdict), screen out function words, and write the steps into build-stages.json.
3. A **pilot round** on a stratified draw, stage 7's machinery unchanged: sense-ranker -> validate -> enricher -> validate -> a complete blind read by entry-reader -> null-auditor on the nulls -> third-hand repairs -> blind re-read.
4. More rounds only if the pilot's reads pass, up to the cap of 4.

Hard stops: the runbook's (any blind read over 5% from 50 senses up; the instrument gate; a tool failing twice; usage limits twice; a live git lock over 15 minutes), plus 10:35.
- plain-004 simplifier (16,725 tokens): 2 tone, 0 keeps. *tawdry* is now "Cheap stuff cheaply made, the whole thing called trash, where *shoddy* is about poor work pretending to be solid." *unreformable* is now "Says only that reform will not work - a practical judgement, without *irredeemable*'s sense of a soul that cannot be saved." Applied, 0 errors. `census-016` (2 notes) and `learner-004` (2 + the 4 controls) drawn, gate pass. **In flight: 1 census-reader, 1 learner-reader.**
- Stage 10 set-up: spaCy 3.8.16 and en_core_web_sm 3.8.0 installed for `book_ingest.py`. The original ingest's versions are not recorded, so the new lemma counts are compared with stage 7's selection before anything is drawn.
- census-016 (10,361 tokens): **2 right, 0 wrong**. learner-004 (12,206 tokens): **2/2 easy, controls 4/4, PASS.** Every note changed tonight has now read right and easy.
- Book one fetched (772,386 bytes, `3f6bb9d6f78e0293`, the recorded id) and ingested in 15 s: 5,288 lemma+POS records, 6,592 sentences. It reproduces stage 7's selection exactly: all 928 entries are present with the same occurrence counts.
- **Stage 10's population, measured** (`tools/book_verdicts.py` -> `data/policy/stage10/population.json`). A sense has a verdict when it ships with a tone note, an explanation or an Enricher's learner line; a SentiWordNet label is not one. Among words with the 3 usable sentences the Ranker needs: **1,683 words, 7,157 open senses**. 526 words / 2,819 senses already have a ranking and need the Enricher only; 1,157 words / 4,338 senses were never opened. The plan's 1,785 / 6,914 was the same count at an earlier commit. 2,507 more words have fewer than 3 usable sentences.
- **Held for the author:** 47 function words, and 66 ranked words with more than 8 open senses (*make* 46, *give* 40). Writing a learner entry for every one of those senses is the concordance the Enricher's cap was built to prevent; a verdict-only pass would need an instrument change. Stage 10's steps are in build-stages.json.
- **Pilot r1** (`tools/stage10_draw.py ranked`, `enrich_packets.py enricher --write-open`): 30 ranked words, stratified over 3 frequency bands (seed 10) from a pool of 413, **89 open senses**, 3 packets. Every open sense is written. **In flight: 3 enrichers.**
- Pilot r1 enrichers back: 3 agents, 83,885 tokens (27,565 / 28,053 / 28,267), about 2,800 a word. `enrich_validate enricher`: **28 accepted, 2 rejected** (*answer*: a 53-word learner line; *introduction*: two learner lines that repeat the gloss). 80 senses written: **77 null, 3 candidate**. Every example on the open senses is invented: these are the senses the book does not use, which is why stage 7 left them. `dict_pipeline --no-build` 0 errors. Cut: 3 reader packets (the 28 entries, complete) and 4 null packets (the 77 nulls). Gate pass. **In flight: 3 entry-readers, 4 null-auditors.**
- 06:52 **All 3 entry-readers failed at once, before reading**: the API refused each request ("Fable 5.1's safeguards flagged this message", details `[reasoning_extraction]`). The null-auditors, also fable, are running normally (packet 04 done: 10,795 tokens). `entry-reader.md` is unchanged since 2026-09-03, when stage 7 used it. The likely trigger is its lines 12-13, "you are not shown what that hand was thinking", read as a request to pull out another model's hidden reasoning. I can't change an instrument (the deny rule) and won't paraphrase one into a prompt. One retry is out, on packet 01. If it fails again, that is the second failure on the same input, and the entry read stops.
- The retry was refused the same way: **the run stops** (a tool failing twice on the same input). The null audit finished as the step in hand: 4 null-auditors, 81,475 tokens, **77 read, 3 false = 3.9%** (stage 7's pilot 2.05%). The three are *evening* "later concluding time period", *love* "have sexual intercourse with" and *loss* "the death of someone", all softenings, so all candidates for the family path.
- **Nothing unread ships.** The pilot's overlay is moved out of `data/entries/overlays/` to `data/policy/stage10-r1/parked.overlay.jsonl` and the batch is rebuilt: byte-identical to before the pilot (`git diff 6550be5`: 0 lines). It comes back once a reader has read it.
- A one-sentence fix waits for the author in `data/policy/instrument-drafts/entry-reader.md` (sha256 `5372ebc0`). "you are not shown what that hand was thinking" becomes "you are shown the entry, not the notes of the hand that wrote it". Nothing else changes. The watchdog is cancelled.

## 06:55 - daytime run report

**Stopped early, at 06:55, on a hard stop**: the entry reader was refused twice by the API's safeguard, so the pilot's enrichment could not be read blind.

Done in the run:
- *tawdry* and *unreformable* rewritten, read right (census 016) and easy (learner 004, controls 4/4). Every note changed tonight now reads right and easy.
- Book one re-ingested; stage 7's 928 selections reproduced exactly.
- Stage 10 measured: 1,683 words and 7,157 senses still open (526 words / 2,819 senses ranked; 1,157 / 4,338 never opened), plus 2,507 words with under 3 usable sentences.
- Pilot r1: 30 words enriched (28 accepted), 80 senses written, null audit 3.9%. The entry read is blocked, so the pilot is parked, not shipped.
- Agents: 1 simplifier, 1 census-reader, 1 learner-reader, 3 enrichers, 4 null-auditors, and 4 entry-reader attempts refused.

**What needs the author:**
1. **Place the entry-reader fix**, the same way learner-reader.md was placed: copy `data/policy/instrument-drafts/entry-reader.md` over `.claude/agents/entry-reader.md` and commit. Then say go: the pilot's read runs first. (Or approve running the entry reader on another model; that is a new baseline.)
2. **How the held words get their verdicts**: 47 function words, 66 words with more than 8 open senses (*make* 46), and 2,507 words with under 3 usable sentences. The Enricher writes a full learner entry for every sense it judges; a verdict-only pass would need a new or changed instrument.
3. *answer* and *introduction* (validator rejects) get re-run at the next go.

## 06:58 - resumed: the entry reader on Sonnet

The author, 06:58: **"Reader on Sonnet for now."** `entry-reader.md` runs verbatim with only its model overridden to Sonnet. It is recorded in build-stages.json as a new baseline: a rate read on Sonnet is not compared with stage 7's Fable reads. The one-sentence fix still waits for the author, to bring the reader back to Fable. The run resumes, stop by 10:35. Next: the pilot's 3 reader packets on Sonnet; the parked overlay comes back only if the read passes.
- Round r2 drawn (`stage10_draw.py new`, no spend yet): 30 never-opened words, stratified from a pool of 1,028, **81 open senses**, 3 ranker packets. Held back: 71 function words and 193 words with more than 8 senses; 2,372 more have under 3 usable sentences. The Ranker starts only if r1's reads pass. *answer* and *introduction* (r1's validator rejects) stay open for a later round.
